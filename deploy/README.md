# Deploy

Deployment manifests, platform config, and release automation files belong here.

Harness validation fails when deployment files such as `Dockerfile`, `vercel.json`,
or Kubernetes/Terraform directories are placed outside this directory.
